services:
  - type: web
    name: myaiproject
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m myaiproject web --production
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: 3.12.10
